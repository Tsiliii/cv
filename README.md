# Curriculum Vitae

LaTeX source for Theodoros Tsilivis's one-page curriculum vitae.

Compile locally with:

```bash
pdflatex main.tex
```

The repository can also be imported directly into Overleaf.
